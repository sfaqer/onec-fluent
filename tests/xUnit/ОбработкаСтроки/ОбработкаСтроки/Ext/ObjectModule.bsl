#Область ОписаниеПеременных

Перем КонтекстЯдра; // Фреймворк тестирования
Перем Ожидаем;      // Ассерты

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

#Область СобытияФреймворкаТестирования

Процедура Инициализация(КонтекстЯдраПараметр) Экспорт
	КонтекстЯдра = КонтекстЯдраПараметр;
	Ожидаем      = КонтекстЯдра.Плагин("УтвержденияBDD");
КонецПроцедуры

Процедура ЗаполнитьНаборТестов(НаборТестов, КонтекстЯдраПараметр) Экспорт
	
	КонтекстЯдра = КонтекстЯдраПараметр;
	
	НаборТестов.Добавить("ТестДолжен_ПолучитьПроцессорКоллекцииИзЗаполненнойСтроки");
	НаборТестов.Добавить("ТестДолжен_ПолучитьПроцессорКоллекцииИзЗаполненнойСтрокиСРазделителем");
	НаборТестов.Добавить("ТестДолжен_ДолженВернутьСтрокуИзСтроки");
	НаборТестов.Добавить("ТестДолжен_ДолженВернутьСтрокуСРазделителемИзСтроки");
	
КонецПроцедуры

#КонецОбласти

#Область Тесты

Процедура ТестДолжен_ПолучитьПроцессорКоллекцииИзЗаполненнойСтроки() Экспорт
	ПроцессорКоллекций = ПроцессорыКоллекций.ИзСтроки("123");
	Результат = ПроцессорКоллекций.ВМассив();
	Ожидаем.Что(Результат).ИмеетДлину(3);
КонецПроцедуры

Процедура ТестДолжен_ПолучитьПроцессорКоллекцииИзЗаполненнойСтрокиСРазделителем() Экспорт
	ПроцессорКоллекций = ПроцессорыКоллекций.ИзСтроки("1 2 3", " ");
	Результат = ПроцессорКоллекций.ВМассив();
	Ожидаем.Что(Результат).ИмеетДлину(3);
КонецПроцедуры

Процедура ТестДолжен_ДолженВернутьСтрокуИзСтроки() Экспорт
	ПроцессорКоллекций = ПроцессорыКоллекций.ИзСтроки("1 2 3", " ");
	Результат = ПроцессорКоллекций.ВСтроку();
	Ожидаем.Что(Результат).ИмеетДлину(3);
КонецПроцедуры

Процедура ТестДолжен_ДолженВернутьСтрокуСРазделителемИзСтроки() Экспорт
	ПроцессорКоллекций = ПроцессорыКоллекций.ИзСтроки("1 2 3", " ");
	Результат = ПроцессорКоллекций.ВСтроку(" ");
	Ожидаем.Что(Результат).ИмеетДлину(5);
КонецПроцедуры

#КонецОбласти

#КонецОбласти
